[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cloud18-ap-analysis"
version = "0.1.0"
description = "Python tools for CLOUD18 atmospheric-physics data analysis"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "matplotlib>=3.7",
    "scipy>=1.10",
]

[tool.setuptools.packages.find]
where = ["src"]
